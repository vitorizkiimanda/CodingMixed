//vector
#include<bits/stdc++.h>
using namespace std;
int main()
{
	vector <int> vektor;
	int x,raw;
	for(int i;i<10;i++)
	{
		cin >> raw;
		vektor.push_back(x); // memasukan data ke vektor
		vektor.pop_back(); // menghapus vektor terakhir
		vektor.clear();//menghapus seluruh vektor
	}
	
	return 0;
}
